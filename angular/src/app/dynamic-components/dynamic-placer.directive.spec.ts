import { DynamicPlacerDirective } from './dynamic-placer.directive';

describe('DynamicPlacerDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicPlacerDirective();
    expect(directive).toBeTruthy();
  });
});
